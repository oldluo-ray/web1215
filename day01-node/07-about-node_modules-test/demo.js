// 如果引入自定义的模块(咱们自己写的js文件), 要写路径
// 如果是第三方的模块/系统自带的模块就直接写模块名
// 如果引入的模块有路径,node就直接从当前项目下,根据路径找我们自定义的模块.
// 如果引入没有路径,node先判断是否是系统自带的模块. 如果不是系统自带,那就自动去当前项目下的node_modules中去查找.
// 如果当前项目下没有node_modules. 会往上一个目录上查找.直到盘符的根目录.如果还没有就报错(找不到)
const uniq = require('uniq')

const arr = uniq([2, 2, 4, 4, 6, 6])
console.log(arr)
