const $ = require('jquery')
console.log($)

const uniq = require('uniq')
const arr = uniq([2, 2, 5, 6, 2, 6, 7, 3])
console.log(arr)

/**
 * 小结:
 *   1. npm 是用来下载管理node平台下包的工具(安装node的时候,就默认下载好了,无需再次下载)
 *   2. 一般要来在我们的项目中下载其他的第三方的库(包)
 *   2.1 第一步:先初始化我们自己的包  npm init -y (注意: 此时包名就是文件夹名称.不要有中文以及大写并且不能和其他的包名相同)
 *   2.2 下载指定的包:
 *          在项目的根目录下,打开命令行工具. 然后执行npm install 包名
 *          如果是下载公司中已经开发过的项目,需要根据package.json/package-lock.json 去下载依赖项. 那么要执行 npm install
 *
 *     注意:有package-lock.json就按照package-lock.json中描述的依赖包,去下载指定版本的依赖包. 如果没有package-lock.json就按照package.json中描述的依赖包去下载最新版
 *
 *
 *
 *      npm 常用指令:
 *
 *         1.  npm install 包名  下载指定包的最新版本
 *         2.  npm install 包名@xx.xx.xx 下载指定包的指定版本
 *         3.  npm i 包名/包名@xx.xx.xx
 *         4.  npm install/i 按照package-lock.json/package.json 下载依赖包
 *         5.  npm remove 包名 删除指定包
 *         6.  npm 下载包的时候,默认使用的是国外的服务器,下载会比较慢
 *          解决办法:在命令行工具中执行这行指令 npm config set registry https://registry.npm.taobao.org.这样下载地址改成了淘宝的镜像地址(国内的服务器)
 *
 *         7. cnpm也是一个包管理工具(下载地址是国内的服务器): 跟npm使用是一样的.只是npm的指令是: npm i 包名.而cnpm的指令改成 cnpm i 包名. 还有一个包管理工具叫yarn
 *
 *         8. npm i 包名 -g  -g表示将这个包下载到当前电脑的全局中(C:\Users\自己的用户名\AppData\Roaming\npm\node_modules)
 *            npm i 包名 没有-g表示下载到本地(就是当前项目的node_modules中)
 *          什么包安装到本地? 一般项目中需要使用到的一些第三方的包(js库),下载到本地
 *          什么包安装到全局? 跟项目无关,是在开发过程中要使用到的一个工具都下载到全局
 *         9. 同时下载多个包:
 *              npm i 包名 包名@xx.xx.xx 包名 包名 (有可能会导致超时)
 *
 *
 */
